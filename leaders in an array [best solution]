// the leaders will be printed from right to left

void leader(int arr[], int n)
{
  int curr_lead=arr[n-1];
  print(curr_lead);
  for(int i=n-2;i>=0;i--)
  {
    if(curr_lead<arr[i])
    {
      curr_lead=arr[i];
      print(curr_lead);
    }
  }
}
